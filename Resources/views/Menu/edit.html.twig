{% extends 'VidelDetailsBundle::base.html.twig' %}

{% form_theme edit_form 'VidelDetailsBundle:Form:fields.html.twig' %}

{% block title %}> Edit of Menu > {{entity.link.title}}{% endblock %}

{% block body %}

<h1>Menu edit</h1>

<form action="{{ path('menu_crud_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('videl_admin_index') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('menu_crud_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{% endblock %}